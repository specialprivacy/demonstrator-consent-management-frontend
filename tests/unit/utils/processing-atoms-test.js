import processingAtoms from 'consent-management-frontend/utils/processing-atoms';
import { module, test } from 'qunit';

module('Unit | Utility | processing-atoms', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = processingAtoms();
    assert.ok(result);
  });
});
