import users from 'consent-management-frontend/utils/users';
import { module, test } from 'qunit';

module('Unit | Utility | users', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = users();
    assert.ok(result);
  });
});
