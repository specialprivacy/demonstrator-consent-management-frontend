import purposeAtoms from 'consent-management-frontend/utils/purpose-atoms';
import { module, test } from 'qunit';

module('Unit | Utility | purpose-atoms', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = purposeAtoms();
    assert.ok(result);
  });
});
