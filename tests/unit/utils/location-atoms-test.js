import locationAtoms from 'consent-management-frontend/utils/location-atoms';
import { module, test } from 'qunit';

module('Unit | Utility | location-atoms', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = locationAtoms();
    assert.ok(result);
  });
});
