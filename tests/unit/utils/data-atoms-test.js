import dataAtoms from 'consent-management-frontend/utils/data-atoms';
import { module, test } from 'qunit';

module('Unit | Utility | data-atoms', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dataAtoms();
    assert.ok(result);
  });
});
