import recipientAtoms from 'consent-management-frontend/utils/recipient-atoms';
import { module, test } from 'qunit';

module('Unit | Utility | recipient-atoms', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = recipientAtoms();
    assert.ok(result);
  });
});
